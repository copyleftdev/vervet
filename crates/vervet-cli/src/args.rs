//! Minimal flag parsing shared by the command modules.

/// Return the value following `flag`, if present.
pub fn value<'a>(args: &'a [String], flag: &str) -> Option<&'a str> {
    args.iter()
        .position(|a| a == flag)
        .and_then(|i| args.get(i + 1))
        .map(String::as_str)
}
